To use browser instead of nodeJS client, client certs must in be in p12 format. 
( p12 contains both key and certificate )

Would the CA (ca/ca.crt) need to be imported into the browser as well? Find out.

client.p12 is for a viers client, other two are proxy aca clients